{
  "name": "50a-ledger",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "TZ=America/Chicago node --test tests/*.test.js tests/*.test.mjs",
    "check": "node --check app.js && node --check recurring-utils.js && node --check sw.js && node --check cloud/ui.mjs",
    "test:rls": "bash scripts/test-rls.sh"
  },
  "dependencies": { "@supabase/supabase-js": "2.117.0" },
  "devDependencies": { "esbuild": "0.28.2" }
}
